rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporarily allow all reads and writes for migration in sandbox
      allow read, write: if true;
    }
  }
}
